int delta(int a, int b, int c) {
  return b*b - 4 * a * c;
}

int pitagoras(int x, int y, int z) {
    if (x*x + y*y = z*z)
        return true;
    else if (x*x + z*z = y*y )
        return true;
    else if (y*y + z*z = x*x)
        return true;
    else
        return false;
}
